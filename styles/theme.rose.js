const colors = require('tailwindcss/colors')

const theme = {
  // Primary color
  alpha: colors.rose,
  // Secondary color
  beta: colors.purple,
  // Accent color
  accent: colors.blue,
  // Neutral color
  omega: colors.zinc,
}

module.exports = theme
