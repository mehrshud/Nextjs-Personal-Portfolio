const colors = require('tailwindcss/colors')

const theme = {
  // Primary color
  alpha: colors.amber,
  // Secondary color
  beta: colors.rose,
  // Accent color
  accent: colors.blue,
  // Neutral color
  omega: colors.zinc,
}

module.exports = theme
